<h1>Editing Book</h1>
<%= form_with model:@book, url 'book_path(@book.id)', method: patch do |f| %>

<label>Tiitle</label>
<%= f.text_field :title %>

<label>Body</label>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>
<%= link_to 'Show', '' %> | <%= link_to 'Back', "" %>
