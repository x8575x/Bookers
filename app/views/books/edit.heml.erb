<h1>Editing Book</h1>
<%= form_with model: @book, url: book_path(:id), method: :patch do |f| %>



<div>
  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>
</div>



<label>Tiitle</label>
<%= f.text_field :title %>

<label>Body</label>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>


<%= link_to "Show", book_path(book.id) %> | <%= link_to 'Back', "/books" %>
